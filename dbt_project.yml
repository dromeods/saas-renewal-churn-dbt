name: 'saas_renewal_churn'
version: '1.0.0'
config-version: 2

profile: 'saas_renewal_churn'

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["data"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  saas_renewal_churn:
    staging:
      +materialized: view
      +tags: ["staging"]
    
    intermediate:
      +materialized: table
      +tags: ["intermediate"]
    
    marts:
      +materialized: table
      +tags: ["marts"]
